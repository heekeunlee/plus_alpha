# 🛡️ ETF LowVol Radar — 저변동성 ETF 랭킹

공공데이터포털 ETF 시세 API를 브라우저에서 직접 호출해 **저변동성 ETF**의
연율화 변동성 · 최대낙폭(MDD) · 위험조정수익률을 계산하고, **변동성이 낮은 순으로 순위**를 매기는 정적 웹앱입니다.

**라이브**: https://heekeunlee.github.io/plus_alpha/

[ETF MomentoScope](https://jgjeong730.github.io/ETF_Momentum/)(모멘텀 랭킹)와 같은 데이터 파이프라인을 재사용하되,
"상승 추세"가 아니라 **"흔들림이 적은 방어형·인컴형 ETF"** 를 찾는 데 초점을 맞췄습니다.

## 핵심 지표

| 지표 | 설명 |
|---|---|
| **연율화 변동성** | 일간수익률 표준편차 × √252 × 100. 1개월·3개월·6개월·1년 윈도우를 각각 계산. **낮을수록 상위 랭크** |
| **최대낙폭 (MDD)** | 최근 1년 내 고점 대비 최대 하락폭(%) |
| **위험조정수익률** | 1년 수익률 ÷ 1년 변동성 (Sharpe와 유사, 높을수록 좋음) |
| **기간별 수익률** | 1개월 / 3개월 / 6개월 / 1년 |
| **분류** | 이름 기반 자동 분류(채권 / 배당·인컴 / 리츠·부동산 / 원자재·금 / 단기·파킹 / 주식·기타) |

## 기능

- **저변동성 랭킹**: 순위 기준 변동성 윈도우(1M/3M/6M/1Y) 선택 → 변동성 오름차순 정렬
- **필터**: 최소 거래대금·최소 시가총액·카테고리, 시총 상위 N개만 히스토리 조회(API 호출 절약)
- **동시성 제한 조회 + 진행률 표시**, 중단 버튼
- **종목찾기**(결과 내 검색) & **관심종목**(localStorage 저장)
- **상세 차트**(Chart.js): 누적수익률 / 가격 / 30일 롤링 변동성 / 낙폭(Drawdown)
- **보유종목 & 구성**: 상세 페이지에 Top10 보유종목·비중, 섹터/자산 구성, 기초지수·운용사·총보수·순자산·상장일 표시
- **모든 컬럼 정렬**, **CSV 내보내기**
- 휴장일 기준일 자동 보정(직전 거래일 탐색)

## 보유종목 데이터 (매일 자동 갱신)

보유종목 정보의 유일한 실용 소스인 Naver Finance는 CORS 헤더가 없어 브라우저에서 직접 호출할 수 없습니다.
그래서 **GitHub Actions**(`.github/workflows/holdings.yml`)가 매일 `scripts/fetch-holdings.js`를 실행해
전 종목 ETF의 보유종목·구성 정보를 수집하고 `data/holdings.json`으로 커밋합니다.
앱은 **같은 도메인의 정적 JSON**을 읽으므로 CORS 문제가 없고 백엔드도 필요 없습니다.

- 갱신 주기: 매일 06:30 KST (수동 실행 `workflow_dispatch`도 가능)
- 보유종목은 원래 실시간으로 바뀌지 않으므로 하루 1회 갱신으로 충분
- 수동 갱신: `node scripts/fetch-holdings.js` 실행 후 `data/holdings.json` 커밋

## 서비스 키

공공데이터포털에서 **[금융위원회 상장지수펀드(ETF)시세정보](https://www.data.go.kr/data/15094808/openapi.do)** 활용신청 후
발급받은 **일반 인증키(Decoding)** 를 앱 상단에 입력합니다.
키는 브라우저 `localStorage`에만 저장되며 어떤 서버로도 전송되지 않습니다(공공데이터포털 API 직접 호출).

## 로컬 실행

정적 파일이라 아무 정적 서버로 실행하면 됩니다.

```
python -m http.server 8000
```

이후 `http://localhost:8000` 접속.

## 배포

`index.html` + `app.js` 두 파일만 정적 호스팅(GitHub Pages 등)에 올리면 동작합니다. 백엔드가 필요 없습니다.

## 면책

본 도구는 정보 제공용이며 투자 권유가 아닙니다. 과거 변동성은 미래 성과를 보장하지 않습니다.

---

데이터: 공공데이터포털 금융위원회 ETF 시세정보 · 차트: Chart.js
